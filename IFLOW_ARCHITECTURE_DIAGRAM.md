# Integration Flow Architecture Diagram

## 🎯 Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         EXTERNAL TRIGGERS                            │
│         (Web UI, Schedulers, External Applications)                  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                     ┌──────────────┼──────────────┐
                     │              │              │
                     ▼              ▼              ▼
    ┌──────────────────────┐  ┌──────────────┐  ┌─────────────────┐
    │  Data Retrieval      │  │ Conflict     │  │ Test/Utility    │
    │  Entry Points        │  │ Resolution   │  │ Entry Points    │
    └──────────────────────┘  └──────────────┘  └─────────────────┘
```

---

## 📊 Main Process Flow: Conflict Resolution

This is your **PRIMARY BUSINESS PROCESS**:

```
┌────────────────────────────────────────────────────────────────────────┐
│                    CONFLICT RESOLUTION PROCESS                          │
└────────────────────────────────────────────────────────────────────────┘

STEP 1: Entry Point
─────────────────────────────────────────────────────────────────────
   External System/User
          │
          ▼
   ┌─────────────────────────────────────┐
   │ Resolve Work Assignment             │
   │ Conflict Main                       │  ← YOU ARE HERE (Main Entry)
   │                                     │
   │ Endpoint: /resolveWAConflictMain    │
   └─────────────────────────────────────┘
          │
          │ Calls internally
          ▼

STEP 2: Get Data
─────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────┐
   │ Get Work Assignment And             │
   │ Timesheet Bulk                      │  ← Retrieves WA & TS data
   │                                     │
   │ Called via: {{CPI_GET_WA_TIME_BULK}}│
   └─────────────────────────────────────┘
          │
          ├─────────────┬─────────────┐
          ▼             ▼             ▼
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │Get List  │  │Get List  │  │Merge     │
   │of Work   │  │of Emp    │  │Results   │
   │Assignment│  │TimeSheet │  │          │
   └──────────┘  └──────────┘  └──────────┘
          │             │             │
          └─────────────┴─────────────┘
                      │
              Returns combined data:
              [{workAssignment, timesheet}, ...]
                      │
                      ▼

STEP 3: Analyze Conflicts
─────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────┐
   │ Resolve Work Assignment             │
   │ Conflict Logic                      │  ← BRAIN: Detects overlaps
   │                                     │
   │ Called via: {{CPI_RESOLVE_WA_CONFLICT}}│
   │                                     │
   │ Script: script1.groovy (145 lines) │
   └─────────────────────────────────────┘
                      │
              Analyzes each pair:
              - WA: 08:00-17:00
              - TS: 07:30-18:00
              → Detects OVERLAP!
                      │
              Returns action plan:
              {
                timesheetDelete: [...],
                workAssignmentDelete: [...],
                timeEventInsert: [...]
              }
                      │
                      ▼

STEP 4: Execute Actions
─────────────────────────────────────────────────────────────────────
   ┌─────────────────────────────────────┐
   │ Resolve Work Assignment             │
   │ Conflict Action                     │  ← EXECUTOR: Fixes conflicts
   │                                     │
   │ Called via: {{CPI_RESOLVE_WA_ACTION}}│
   └─────────────────────────────────────┘
          │
          ├────────────┬────────────┬────────────┐
          ▼            ▼            ▼            ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
   │ Delete   │ │ Insert   │ │ Delete   │ │ Return   │
   │Timesheets│ │New Time  │ │Work      │ │Success   │
   │(PATCH)   │ │Events    │ │Assign.   │ │Response  │
   │          │ │(POST)    │ │(POST)    │ │          │
   └──────────┘ └──────────┘ └──────────┘ └──────────┘
       │             │             │
       ▼             ▼             ▼
   ┌────────────────────────────────────┐
   │   SAP SuccessFactors               │
   │   - TimeEvent API                  │
   │   - Employee Time API              │
   └────────────────────────────────────┘
```

---

## 🔄 Detailed Flow Connections

### 1. CONFLICT RESOLUTION FLOWS (Main Process)

```
┌─────────────────────────────────────────────────────────────────┐
│ External Trigger (HTTP/Scheduler)                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌────────────────────────────────────────┐
        │ Resolve Work Assignment Conflict Main  │
        │ (ORCHESTRATOR)                         │
        │ ──────────────────────────────────     │
        │ Endpoint: /resolveWAConflictMain       │
        │ Trigger: HTTP POST                     │
        └────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼                               ▼
    ┌──────────────────────┐      ┌──────────────────────┐
    │ Get Work Assignment  │      │ Resolve WA Conflict  │
    │ And Timesheet Bulk   │──────│ Logic                │
    │                      │      │ (ANALYZER)           │
    │ Returns: Combined    │      │ ──────────────────   │
    │ WA + TS data         │      │ Detects overlaps     │
    └──────────────────────┘      │ Generates actions    │
              │                   └──────────────────────┘
              │                               │
              └───────────────┬───────────────┘
                              ▼
                  ┌────────────────────────┐
                  │ Resolve WA Conflict    │
                  │ Action                 │
                  │ (EXECUTOR)             │
                  │ ──────────────────     │
                  │ Deletes timesheets     │
                  │ Inserts new events     │
                  │ Deletes work assign.   │
                  └────────────────────────┘
                              │
                              ▼
                  ┌────────────────────────┐
                  │ Delete Work Assignment │
                  │ (HELPER)               │
                  │ ──────────────────     │
                  │ Upserts to SF          │
                  └────────────────────────┘
                              │
                              ▼
                  ┌────────────────────────┐
                  │ SuccessFactors APIs    │
                  └────────────────────────┘
```

### 2. DATA RETRIEVAL FLOWS (Supporting the Main Process)

```
GET WORK ASSIGNMENT & TIMESHEET BULK
────────────────────────────────────────────────────────────────

┌────────────────────────────────────┐
│ Get Work Assignment And            │
│ Timesheet Bulk                     │
│ ────────────────────────────       │
│ Endpoint: /getWAAndTimesheetBulk   │
│ Purpose: Retrieve bulk WA + TS     │
└────────────────────────────────────┘
                │
    ┌───────────┴───────────┐
    ▼                       ▼
┌─────────────────┐   ┌─────────────────┐
│ Get List of     │   │ Get List of     │
│ work Assignment │   │ Employee        │
│                 │   │ TimeSheet       │
│ ───────────     │   │ ───────────     │
│ Calls SF:       │   │ Calls SF:       │
│ Employee Time   │   │ TimeEvent API   │
│ API             │   │ (C10/C20)       │
│                 │   │                 │
│ Filter: Last    │   │ Filter: Date    │
│ 2 months to     │   │ range + Type    │
│ current month   │   │                 │
└─────────────────┘   └─────────────────┘
        │                     │
        └──────────┬──────────┘
                   ▼
         ┌──────────────────┐
         │ Merge & Combine  │
         │ (script5.groovy) │
         └──────────────────┘
                   │
          Returns: JSON Array
          [{
            workAssignment: {id, employeeId, date, startTime, endTime},
            timesheet: {checkIn, checkOut}
          }, ...]
```

```
GET LOCATION WORK ASSIGNMENT LIST
────────────────────────────────────────────────────────────────

┌────────────────────────────────────┐
│ Get Location work Assignment List  │
│ ────────────────────────────       │
│ Endpoint: /getWAAndTimesheet       │
│ Input: location parameter          │
└────────────────────────────────────┘
                │
                ▼
        ┌──────────────┐
        │ Get Work     │
        │ Assignment   │──────┐
        │ (via CPI)    │      │
        └──────────────┘      │
                │             │
                ▼             │
        ┌──────────────┐      │
        │ Convert to   │      │
        │ XML          │      │
        │ (script1)    │      │
        └──────────────┘      │
                │             │
                ▼             │
        ┌──────────────┐      │
        │ LOOP THROUGH │      │
        │ EACH RECORD  │      │ For each employee:
        │ (Splitter)   │      │
        └──────────────┘      │
                │             │
                ▼             │
        ┌──────────────┐      │
        │ Extract      │      │
        │ EmployeeId   │      │
        │ & Date       │      │
        │ (script2)    │      │
        └──────────────┘      │
                │             │
                ▼             │
        ┌──────────────┐      │
        │ Get Employee │◄─────┘
        │ TimeSheet    │ (called per employee)
        │ (via CPI)    │
        └──────────────┘
                │
                ▼
        ┌──────────────┐
        │ Combine WA   │
        │ + TS         │
        │ (script3)    │
        └──────────────┘
                │
                ▼
        ┌──────────────┐
        │ GATHER ALL   │
        │ RESULTS      │
        │ (Gather)     │
        └──────────────┘
                │
        Returns: Combined XML
        <Payload>
          <WorkAssignment>...</WorkAssignment>
          <Timesheet>...</Timesheet>
        </Payload>
```

### 3. STANDALONE UTILITY FLOWS

```
┌────────────────────────────────────┐
│ Get Employee List By Location      │
│ ────────────────────────────       │
│ Endpoint: /getEmployeeByLocation   │
│ Input: location=<id>               │
│ Output: Employee list              │
│ Calls: SF EmpJob OData API         │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ Delete Work Assignment             │
│ ────────────────────────────       │
│ Endpoint: /deleteWAList            │
│ Input: Work assignment IDs         │
│ Output: Deletion confirmation      │
│ Calls: SF Upsert API               │
│ Action: Sets approvalStatus=       │
│         CANCELLED                  │
└────────────────────────────────────┘
```

### 4. TEST FLOWS

```
┌────────────────────────────────────┐
│ test-Create Employee Timesheet     │
│ ────────────────────────────       │
│ Endpoint: /createEmployeeTimesheet │
│ Input: {EmpId, Date, Checkin,      │
│         Checkout, Timezone}        │
│ Creates: C10 (check-in) and        │
│          C20 (check-out) events    │
│ Calls: SF TimeEvent API            │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ test-Delete Employee Timesheet     │
│ ────────────────────────────       │
│ Endpoint: /deleteEmployeeTimesheet │
│ Same as create (likely uses diff   │
│ SF endpoint for delete operation)  │
└────────────────────────────────────┘
```

---

## 🗺️ Complete Dependency Map

```
LEGEND:
──────► Calls (HTTP request)
······> Data flow
[SF] = SuccessFactors API
[CPI] = Internal CPI iFlow
```

```
External System
      │
      ▼
┌─────────────────────────────────────────────────┐
│ ORCHESTRATORS (Entry Points)                    │
├─────────────────────────────────────────────────┤
│                                                  │
│ [1] Resolve WA Conflict Main                    │
│     │                                            │
│     ├──────────────► Get WA And TS Bulk [CPI]   │
│     │                    │                       │
│     │                    ├─────► Get List of WA [CPI]
│     │                    │           └─────► [SF] Employee Time API
│     │                    │                       │
│     │                    └─────► Get List of Emp TS [CPI]
│     │                                └─────► [SF] TimeEvent API
│     │                                            │
│     ├──────────────► Resolve WA Logic [CPI]     │
│     │                    │                       │
│     │                    └──► (Groovy script     │
│     │                          analyzes data)    │
│     │                                            │
│     └──────────────► Resolve WA Action [CPI]    │
│                          │                       │
│                          ├─────► [SF] TimeEvent API (DELETE)
│                          ├─────► [SF] TimeEvent API (INSERT)
│                          └─────► Delete WA [CPI]
│                                      └─────► [SF] Upsert API
│                                                  │
│ [2] Get Location WA List                        │
│     │                                            │
│     ├──────────────► Get WA [CPI]               │
│     │                    └─────► [SF] Employee Time API
│     │                                            │
│     └──────────────► (Loop) Get Emp TS [CPI]    │
│                          └─────► [SF] TimeEvent API
│                                                  │
│ [3] Work Assignment Conflict Main (duplicate)   │
│     │                                            │
│     └──────────────► Same as [1] above          │
│                                                  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ STANDALONE/UTILITY FLOWS                        │
├─────────────────────────────────────────────────┤
│                                                  │
│ Get Employee List By Location                   │
│     └─────────────► [SF] EmpJob OData API       │
│                                                  │
│ Delete Work Assignment                          │
│     └─────────────► [SF] Upsert API             │
│                                                  │
│ test-Create Employee Timesheet                  │
│     └─────────────► [SF] TimeEvent API          │
│                                                  │
│ test-Delete Employee Timesheet                  │
│     └─────────────► [SF] TimeEvent API          │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## 🎭 Flow Roles and Responsibilities

| Flow Name | Role | Called By | Calls To | SuccessFactors Impact |
|-----------|------|-----------|----------|---------------------|
| **Resolve WA Conflict Main** | Orchestrator | External | 3 CPI flows | ✅ Yes (via actions) |
| **Resolve WA Conflict Logic** | Analyzer | Orchestrator | None | ❌ No (read-only logic) |
| **Resolve WA Conflict Action** | Executor | Orchestrator | Delete WA flow + SF APIs | ✅ Yes (writes) |
| **Get WA And TS Bulk** | Data Aggregator | Orchestrator | 2 CPI flows | ❌ No (read-only) |
| **Get List of WA** | SF Reader | Aggregator | SF API | ❌ No (read-only) |
| **Get List of Emp TS** | SF Reader | Aggregator | SF API | ❌ No (read-only) |
| **Get Location WA List** | Orchestrator | External | 2 CPI flows | ❌ No (read-only) |
| **Delete Work Assignment** | SF Writer | Action flow | SF API | ✅ Yes (deletes) |
| **Get Employee List By Loc** | Utility | External | SF API | ❌ No (read-only) |
| **test-Create Emp TS** | Test | External | SF API | ✅ Yes (creates) |
| **test-Delete Emp TS** | Test | External | SF API | ✅ Yes (deletes) |

---

## 📈 Data Flow Through the System

### Example: Resolving a Conflict

```
INPUT:
────────────────────────────────────────────────────────
Employee: 30933
Date: 2025-11-06

From SuccessFactors:
  Work Assignment: 08:00 - 17:00
  Timesheet: 07:30 - 18:00

  ❌ OVERLAP DETECTED!

────────────────────────────────────────────────────────
STEP 1: Get Work Assignment And Timesheet Bulk
────────────────────────────────────────────────────────
  Retrieves from SF:
  {
    "workAssignment": {
      "id": "WA12345",
      "employeeId": "30933",
      "date": "2025-11-06",
      "startTime": "08:00",
      "endTime": "17:00"
    },
    "timesheet": {
      "employeeId": "30933",
      "date": "2025-11-06",
      "checkIn": { "id": "TS001", "timeLocal": "07:30" },
      "checkOut": { "id": "TS002", "timeLocal": "18:00" }
    }
  }

────────────────────────────────────────────────────────
STEP 2: Resolve Work Assignment Conflict Logic
────────────────────────────────────────────────────────
  Analyzes:
    WA: 08:00 ─────────────────────► 17:00
    TS: 07:30 ─────────────────────────────► 18:00
        └─overlap─┘                  └─overlap─┘

  Rule Applied: Partial overlap (TS extends both sides)
  Decision: Trim timesheet checkout to 07:59
            (WA start - 1 minute)

  Generates Actions:
  {
    "timesheetDelete": {
      "value": [{ "id": "TS002", "deleted": true }]
    },
    "workAssignmentDelete": { "value": [] },
    "timeEventInsert": [{
      "id": "2",
      "assignmentId": "30933",
      "typeCode": "C20",
      "timestamp": "2025-11-06T07:59:00+0300",
      "terminalId": "Manual",
      "timeTypeCode": "WORKING_TIME"
    }]
  }

────────────────────────────────────────────────────────
STEP 3: Resolve Work Assignment Conflict Action
────────────────────────────────────────────────────────
  1. DELETE timesheet TS002 (old check-out)
     → PATCH to SF TimeEvent API

  2. INSERT new timesheet event (new check-out at 07:59)
     → POST to SF TimeEvent API

  3. (No work assignment deletions in this case)

  Result: ✅ Conflict resolved!

────────────────────────────────────────────────────────
FINAL STATE IN SUCCESSFACTORS:
────────────────────────────────────────────────────────
  Work Assignment: 08:00 ───────► 17:00
  Timesheet:       07:30 ──► 07:59

  ✅ NO OVERLAP - CONFLICT RESOLVED!
```

---

## 🔑 Key Integration Points

### External Endpoints (Configurable Properties)

| Property | Purpose | Example |
|----------|---------|---------|
| `{{SF_EMPLOYEE_TIME}}` | Work assignment data | https://api.sapsf.com/.../EmployeeTime |
| `{{SF_CLOCK_IN}}` | TimeEvent read/delete | https://api.sapsf.com/.../TimeEvent |
| `{{SF_TIME_EVENT}}` | TimeEvent create | https://api.sapsf.com/.../TimeEvent |
| `{{SF_UPSERT}}` | Upsert operations | https://api.sapsf.com/.../upsert |
| `{{CPI_GET_WA_TIME_BULK}}` | Internal CPI call | https://cpi.sap.com/.../getWAAndTimesheetBulk |
| `{{CPI_RESOLVE_WA_CONFLICT}}` | Internal CPI call | https://cpi.sap.com/.../resovleWAConfilict |
| `{{CPI_RESOLVE_WA_ACTION}}` | Internal CPI call | https://cpi.sap.com/.../resolveWAConflictAction |

---

## 💡 Summary

### Primary Business Process:
**Resolve Work Assignment Conflict Main** → This is your main entry point

### Supporting Flows:
- **Data Retrieval**: Get bulk WA + TS data
- **Analysis**: Detect overlaps and decide actions
- **Execution**: Apply fixes to SuccessFactors

### Standalone Utilities:
- Employee list by location
- Delete work assignments
- Test flows for development

### Duplicate Flows (Can Be Deleted):
- ❌ Work Assignment Conflict Main (duplicate of Resolve WA Conflict Main)
- ❌ Get Employee TimeSheet (duplicate of Get List of Employee TimeSheet)
